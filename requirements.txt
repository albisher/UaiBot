# filepath: /Users/amac/Documents/code/Labeeb/requirements.txt

# Labeeb requirements are now managed in setup.py
# Please install using:
#   pip install .
# For development tools:
#   pip install .[dev]
# For testing tools:
#   pip install .[test]

# Core dependencies
aiohttp>=3.12.0
pyautogui>=0.9.54
psutil>=7.0.0
pillow>=11.2.1
pytesseract>=0.3.10
opencv-python>=4.8.0
numpy>=1.24.0
websockets>=12.0
ollama>=0.1.7
transformers>=4.40.0
pyjwt>=2.8.0
bcrypt>=4.1.2
orjson>=3.9.10
requests>=2.31.0
urllib3>=2.0.0
cryptography>=41.0.0
toml>=0.10.2
colorama>=0.4.6

# RTL and Arabic language support
arabic-reshaper>=3.0.0
python-bidi>=0.4.2

# Development dependencies
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.7.0
isort>=5.12.0
flake8>=6.1.0
mypy>=1.5.1
pytest-asyncio>=0.18.0
python-json-logger>=2.0.7
colorlog>=6.7.0

# GUI dependencies
PyQt5>=5.15.11
PyQt5-sip>=12.17.0
PyQt5-Qt5>=5.15.17
pyaudio>=0.2.13

# Audio and Speech
pyttsx3>=2.90
whisper>=1.1.10
openai-whisper @ git+https://github.com/openai/whisper.git

# Platform specific dependencies
# macOS
pyobjc; sys_platform == 'darwin'  # This will install all required pyobjc packages
pyobjc-framework-AVFoundation; sys_platform == 'darwin'
pyobjc-framework-AppKit; sys_platform == 'darwin'
pyobjc-framework-CoreAudio; sys_platform == 'darwin'
pyobjc-framework-Foundation; sys_platform == 'darwin'
pyobjc-framework-IOBluetooth; sys_platform == 'darwin'
pyobjc-framework-IOKit; sys_platform == 'darwin'
pyobjc-framework-Quartz; sys_platform == 'darwin'
objc; sys_platform == 'darwin'

# Windows
pywin32>=306; sys_platform == 'win32'
wmi>=1.5.1; sys_platform == 'win32'
comtypes>=1.2.0; sys_platform == 'win32'
win32api; sys_platform == 'win32'
win32clipboard; sys_platform == 'win32'
win32com; sys_platform == 'win32'
win32con; sys_platform == 'win32'
win32file; sys_platform == 'win32'
win32gui; sys_platform == 'win32'
win32process; sys_platform == 'win32'
win32security; sys_platform == 'win32'

# Linux
python-xlib>=0.33; sys_platform == 'linux'
pyalsaaudio>=0.10.0; sys_platform == 'linux'  # Provides alsaaudio module
alsaaudio>=0.10.0; sys_platform == 'linux'  # Alias for pyalsaaudio (for compatibility)
pyudev; sys_platform == 'linux'
distro; sys_platform == 'linux'
sounddevice; sys_platform == 'linux'

# Additional utilities
ffmpeg-python>=0.2.0
soundfile>=0.12.1
PyYAML>=6.0.1
selenium>=4.18.1

# Core AI and agent framework
smolagents

# LLM and embedding
transformers
sentence-transformers
torch

# Vector DB for memory
chromadb

# Browser automation
playwright
selenium

# System and device awareness
psutil
pyautogui
pynput

# File, network, and OS tools
requests
bs4

# i18n and translation
gettext

# Audio and vision
pyaudio
pillow
opencv-python

# For TTS/STT
pyttsx3
openai-whisper

# For testing and linting
pytest
pytest-asyncio

# For parsing and config
python-dotenv
toml

# For documentation
mkdocs

# For Linux-specific features (documented)
# sudo apt install ffmpeg (for audio)
# sudo apt install tesseract-ocr (for OCR)

# Cross-platform/other
Xlib
cv2
pygetwindow
screen_brightness_control
usb
matplotlib
networkx
spacy
langdetect
keyboard
jwt
aiomysql
aioimaplib
aiosmtplib
dns
